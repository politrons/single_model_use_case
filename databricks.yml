# The name of the bundle. run `databricks bundle schema` to see the full bundle settings schema.
bundle:
  # Do not modify the below line, this autogenerated field is used by the Databricks backend.
  uuid: ${databricks_uuid}
  name: single_model_use_case

variables:
  workspace_name:
    description: Workspace name for the user.
    default: /Workspace/Users/${workspace.current_user.userName}/.bundle
  catalog_name:
    description: The catalog name to save the trained model
include:
  - ./resources/factory-tuning-model-use-case-1-resource.yml

# Deployment Target specific values for workspace
targets:
  dev:
    mode: development
    default: true
    presets:
      source_linked_deployment: false
    variables:
      catalog_name: axainsurance_claims_dv_ml
    workspace:
      host: https://adb-3446814781423407.7.azuredatabricks.net
  staging:
    variables:
      catalog_name: axainsurance_claims_dv_ml
    workspace:
      host: https://adb-3446814781423407.7.azuredatabricks.net
  prod:
    variables:
      catalog_name: axainsurance_claims_dv_ml
    workspace:
      host: https://adb-3446814781423407.7.azuredatabricks.net
